FROM nginx:latest
COPY <<EOF /usr/share/nginx/html/index.html
<h1>Página Personalizada do Minicurso de Dockerfile</h1>
EOF
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
HEALTHCHECK --interval=30s --timeout=10s --retries=3 CMD curl -f http://localhost:80 || exit 1
